# Plan Third Review

独立审阅并在必要时直接修正 `TARGET_FILE`。`SPEC_FILE` 只读，禁止修改关联 spec。只允许修改目标 plan，不得修改、创建、删除、重命名或改变其它文件及 Git index，也不得改变 spec 已批准的设计决策。

覆盖 spec 要求的逐条追踪、任务依赖与可运行顺序、真实文件路径、既有接口签名、TDD 的 RED/GREEN 命令以及验收追踪和可执行性。只统计真实的语义修正。发现并修复 20 个以上真实问题是审阅强度目标，不是造数目标；真实问题不足 20 个时，说明不足原因和已覆盖的审阅维度。禁止 emoji、夸张格式和营销文案。

stdout 去除整体首尾空白后必须恰好为以下五个唯一字段，不得添加日志、说明或 Markdown 围栏：

```text
状态: approved | fixed
修改数量: N 项
修改摘要: 1-5 条；approved 时填写“无”
目标未达说明: 无 | N 小于 20 的真实原因和已覆盖审阅维度
剩余风险: 无 | 1-3 条
```

N 必须是十进制非负整数。`approved` 仅用于 N=0 且目标内容未变；`fixed` 仅用于 N>=1 且目标内容已变。修改摘要和剩余风险的条目用全角分号 `；` 分隔且不得为空。N<20 时目标未达说明必须为非空、非“无”的自然语言，语义上说明真实原因和已覆盖审阅维度；该语义由 provider 负责并由后续主流程模型复审。N>=20 时目标未达说明必须为“无”。诊断只能写 stderr，不得泄露凭据、完整环境或目标全文。
